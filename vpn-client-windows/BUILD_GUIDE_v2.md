# Руководство по сборке NovaVPN v2.0.0

## 🚀 Быстрый старт

### Простой запуск (рекомендуется)

Просто дважды щелкните по файлу:
```
build-v2.bat
```

Скрипт автоматически:
- Проверит все зависимости
- Соберет приложения
- Создаст инсталлятор
- Подготовит дистрибутив

**Не требует никаких аргументов!**

---

## 📋 Что делает скрипт

### Шаги сборки (8 этапов)

1. **Проверка зависимостей**
   - Go 1.21+ (обязательно)
   - go-winres (устанавливается автоматически)
   - Inno Setup 6 (для создания .exe инсталлятора)
   - wintun.dll (предупреждает если отсутствует)

2. **Загрузка зависимостей Go**
   - `go mod tidy`

3. **Генерация иконок**
   - Из SVG в встраиваемый формат

4. **Встраивание ресурсов GUI**
   - Иконки, манифест, информация о версии 2.0.0

5. **Встраивание ресурсов Service**
   - Манифест, информация о версии 2.0.0

6. **Компиляция**
   - NovaVPN.exe (GUI приложение)
   - novavpn-service.exe (Windows служба)

7. **Подготовка дистрибутива**
   - Копирование в `dist/NovaVPN/`
   - Создание README.txt

8. **Создание инсталлятора**
   - `dist/NovaVPN-Setup-2.0.0.exe`

---

## 📦 Результаты сборки

После успешной сборки вы получите:

```
vpn-client-windows/
├── NovaVPN.exe                     (в корне)
├── novavpn-service.exe             (в корне)
└── dist/
    ├── NovaVPN/                    (папка для ручного распространения)
    │   ├── NovaVPN.exe
    │   ├── novavpn-service.exe
    │   ├── wintun.dll
    │   └── README.txt
    └── NovaVPN-Setup-2.0.0.exe     (инсталлятор)
```

---

## ⚙️ Требования

### Обязательные

- **Windows 10/11** (64-bit)
- **Go 1.21+** - https://go.dev/dl/
- **wintun.dll** - https://www.wintun.net/ (amd64)

### Опциональные

- **Inno Setup 6** - для создания .exe инсталлятора
  - Скачать: https://jrsoftware.org/isdl.php
  - Или: `winget install JRSoftware.InnoSetup`

### Автоматически устанавливаемые

- **go-winres** - устанавливается скриптом если отсутствует

---

## 🔍 Что делать если...

### Go не найден

```
[✗] Go не найден
    Установите Go 1.21+ с https://go.dev/dl/
```

**Решение:**
1. Скачайте и установите Go с https://go.dev/dl/
2. Перезапустите терминал
3. Запустите build-v2.bat снова

---

### wintun.dll отсутствует

```
[!] wintun.dll не найден
    Скачайте с https://www.wintun.net/ (amd64)
```

**Решение:**
1. Перейдите на https://www.wintun.net/
2. Скачайте последнюю версию
3. Извлеките `wintun.dll` из архива (папка `amd64/`)
4. Скопируйте в корень `vpn-client-windows/`
5. Запустите build-v2.bat снова

**Примечание:** Сборка продолжится без wintun.dll, но приложение не будет работать без него!

---

### Inno Setup не найден

```
[✗] Inno Setup 6 не найден
    Установите с https://jrsoftware.org/isdl.php
```

**Что произойдет:**
- Сборка продолжится
- Будут созданы .exe файлы
- Папка `dist/NovaVPN/` будет подготовлена
- НО инсталлятор `.exe` НЕ будет создан

**Решение (если нужен инсталлятор):**
1. Установите Inno Setup 6:
   ```
   winget install JRSoftware.InnoSetup
   ```
   Или скачайте с https://jrsoftware.org/isdl.php

2. Запустите build-v2.bat снова

**Альтернатива:**
Используйте папку `dist/NovaVPN/` для ручного распространения (без инсталлятора)

---

### Ошибка компиляции

```
[✗] Ошибка сборки GUI
```

**Решение:**
1. Убедитесь что `go mod tidy` выполнился успешно
2. Проверьте подключение к интернету (для загрузки зависимостей)
3. Проверьте что проект не изменен (нет ошибок в коде)
4. Попробуйте вручную:
   ```bash
   go build -o NovaVPN.exe ./cmd/novavpn/
   ```

---

## 📝 Детали версионирования

Версия **2.0.0** встраивается в:

| Файл | Где обновлено |
|------|---------------|
| NovaVPN.exe | `winres/gui.json` |
| novavpn-service.exe | `winres/winres.json` |
| Инсталлятор | `installer/novavpn.iss` |
| Имя установщика | `NovaVPN-Setup-2.0.0.exe` |

Информация о версии видна в:
- Свойствах файла (ПКМ → Свойства → Подробно)
- Панели управления Windows (Программы и компоненты)
- Диспетчере задач

---

## 🧪 Тестирование после сборки

### Вариант 1: Запуск из корня

```bash
cd vpn-client-windows
NovaVPN.exe
```

### Вариант 2: Запуск дистрибутива

```bash
cd dist\NovaVPN
NovaVPN.exe
```

### Вариант 3: Установка через инсталлятор

1. Дважды щелкните `dist\NovaVPN-Setup-2.0.0.exe`
2. Следуйте инструкциям установщика
3. Запустите из меню "Пуск"

---

## 🔄 Пересборка

Если нужно пересобрать:

1. **Удалите старые файлы:**
   ```bash
   del NovaVPN.exe
   del novavpn-service.exe
   rmdir /S /Q dist
   ```

2. **Запустите сборку:**
   ```bash
   build-v2.bat
   ```

---

## 📊 Сравнение со старыми скриптами

| Функция | build.bat (старый) | build-v2.bat (новый) |
|---------|-------------------|---------------------|
| Компиляция | ✓ | ✓ |
| Ресурсы | ✓ | ✓ |
| Дистрибутив | ✗ | ✓ |
| Инсталлятор | Отдельный скрипт | ✓ Включен |
| Версия | 1.0.0 | 2.0.0 |
| Информативность | Средняя | Высокая |
| Проверки | Базовые | Полные |
| README | ✗ | ✓ Автоматически |

---

## 💡 Советы

### Для разработчиков

- Скрипт можно запускать многократно
- Старые файлы перезаписываются
- Ресурсы пересоздаются каждый раз
- Git игнорирует `.exe` и `dist/` (см. `.gitignore`)

### Для распространения

**Вариант 1: Инсталлятор (рекомендуется)**
- Распространяйте `NovaVPN-Setup-2.0.0.exe`
- Пользователи просто запускают .exe
- Автоматическая установка службы
- Создание ярлыков

**Вариант 2: ZIP архив**
- Заархивируйте папку `dist/NovaVPN/`
- Пользователи распаковывают и запускают
- Требуется ручная установка службы

---

## 🆘 Получение помощи

Если возникли проблемы:

1. **Проверьте вывод скрипта** - он подробно описывает что происходит
2. **Посмотрите секцию "Что делать если..."** выше
3. **Проверьте зависимости:**
   ```bash
   go version
   go-winres --version
   iscc /?
   ```
4. **Откройте issue** на GitHub с полным выводом скрипта

---

## 📚 Связанная документация

- `README.md` - Основная документация проекта
- `FOLDER_STRUCTURE_ANALYSIS.md` - Анализ структуры
- `REFACTORING_PROGRESS.md` - Прогресс рефакторинга
- `installer/novavpn.iss` - Скрипт Inno Setup

---

**Дата создания:** 13 февраля 2026  
**Версия документа:** 1.0  
**Версия приложения:** 2.0.0
