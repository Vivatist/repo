# Анализ стойкости NovaVPN к обнаружению и блокировке

> Дата анализа: 12 февраля 2026  
> Объект: NovaVPN Protocol v2 (stealth)  
> Фокус: способность протокола оставаться незамеченным для DPI провайдера

---

## 1. Модель угрозы

Провайдер (ISP) может применять следующие методы обнаружения:

| Уровень | Метод | Пример |
|---------|-------|--------|
| L1 | Блокировка по порту | Закрытие UDP/443 |
| L2 | Простая DPI (pattern matching) | Поиск сигнатуры первых байтов |
| L3 | Stateful DPI (отслеживание состояний) | Анализ последовательности пакетов, ожидание TLS handshake |
| L4 | Протокольная валидация | Проверка: «Является ли это валидным TLS/DTLS/QUIC?» |
| L5 | Статистический анализ | Распределение размеров пакетов, межпакетные интервалы |
| L6 | Активное зондирование (active probing) | Подключение к серверу, проверка реакции |

---

## 2. Критическая уязвимость: UDP + TLS 1.2

### Проблема

```
Протокол:  UDP-порт 443
Заголовок: 0x17 0x03 0x03 ... (TLS 1.2 Application Data)
```

**TLS 1.2 работает исключительно поверх TCP.** Появление TLS Record Header в UDP-пакете — **аномалия, которой не существует в природе**.

Провайдер может детектировать это одним правилом:

```
IF transport == UDP AND port == 443 AND payload[0] == 0x17 AND payload[1:3] == 0x0303
THEN → подозрительный трафик (не TLS, не DTLS, не QUIC)
```

### Что существует на UDP/443 в реальности:

| Протокол | Первые байты | Описание |
|----------|-------------|----------|
| **QUIC v1** | `0x00..0x3F` (short header) или `0xC0+` (long header) | Google, HTTP/3 |
| **DTLS 1.2** | `0x17 0x03 0x03` НЕТ — DTLS использует `0xFEFD` | Голос, WebRTC |
| **DTLS 1.0** | ContentType `0x17`, но version `0xFEFF` | Устаревший |

DTLS (единственный легитимный «TLS over UDP») использует **другие номера версий**: `0xFEFD` (DTLS 1.2) вместо `0x0303` (TLS 1.2). Провайдер, сверяющий таблицу версий, немедленно обнаружит несоответствие.

**Оценка: 2/10** — отсечение одной проверкой.

---

## 3. Отсутствие TLS Handshake

### Проблема

В реальном TLS/DTLS **любая сессия** начинается с ContentType `0x16` (Handshake):
```
Настоящий TLS:   [0x16 ...ClientHello] → [0x16 ...ServerHello] → ... → [0x17 ...Data]
NovaVPN:         [0x17 ...HandshakeInit]  ← первый же пакет = Application Data
```

Провайдер с stateful DPI:
```
IF first_packet.contentType == 0x17 (Application Data)
   AND no prior 0x16 (Handshake) packets in this flow
THEN → не настоящий TLS
```

Этот метод используется в Китае (GFW), Иране, России (ТСПУ) для обнаружения обфускированных протоколов.

**Оценка: 1/10** — тривиальная детекция через stateful анализ.

---

## 4. Фиксированные паттерны в открытой части пакета

### 4.1. PacketType на фиксированной позиции

Байт `data[9]` (смещение 4 от начала данных после TLS header) — тип пакета:

```
HandshakeInit:     data[9] = 0x01
HandshakeResp:     data[9] = 0x02
HandshakeComplete: data[9] = 0x03
Data:              data[9] = 0x10
Keepalive:         data[9] = 0x20
Disconnect:        data[9] = 0x30
Error:             data[9] = 0xF0
```

В реальном TLS 1.2 Application Data после Record Header идёт зашифрованный блок (GCM nonce / CBC IV) — **байт на позиции 9 равномерно распределён** (выглядит случайно). В NovaVPN он принимает лишь **7 значений** из 256. Это создаёт ярко выраженный fingerprint:

```python
# DPI-правило
suspicious = packet[9] in {0x01, 0x02, 0x03, 0x10, 0x20, 0x30, 0xF0}
# Вероятность ложного срабатывания: 7/256 = 2.7%
# После 3+ пакетов: (7/256)^3 = 0.002% → практически нулевая
```

**Оценка: 2/10**

### 4.2. Нулевой Nonce в HandshakeInit

Первый пакет клиента:
```
Байты [10:22] = 0x00 0x00 0x00 0x00 0x00 0x00 0x00 0x00 0x00 0x00 0x00 0x00
```

12 нулевых байт подряд на фиксированной позиции — **абсолютная аномалия** для зашифрованного трафика. DPI может обнаружить это с одного пакета.

**Оценка: 1/10**

### 4.3. SessionID = 0 в первом пакете

```
Байты [5:9] = 0x00 0x00 0x00 0x00
```

4 нулевых байта + 12 нулевых байт = **16 нулевых байт подряд** (позиции 5-21 в пакете). В зашифрованном TLS-трафике вероятность такого события: $\frac{1}{2^{128}} \approx 0$.

**Оценка: 0/10** — мгновенная идентификация первого пакета.

---

## 5. Фиксированные размеры служебных пакетов

### 5.1. Keepalive

```
Keepalive = TLS_Header(5) + SessionID(4) + Type(1) + Nonce(12) = 22 байта
```

Каждые 20-35 секунд появляются UDP-пакеты ровно по **22 байта** на порт 443. Это уникальная сигнатура.

В реальном TLS keepalive — это TCP-уровневый механизм (без payload). В DTLS heartbeat — ContentType `0x18`, минимум 19 байт. В QUIC — пинг-фреймы произвольного размера с padding.

### 5.2. Disconnect

```
Disconnect = 22 байта (аналогично keepalive)
```

### 5.3. HandshakeComplete

```
HandshakeComplete = 22 + ChaCha20(HMAC(32)) = 22 + 48 = 70 байт (фиксированно)
```

### 5.4. HandshakeInit

```
HandshakeInit = 22 + ClientPubKey(32) + Timestamp(8) + CredsLen(2) + EncCreds(var) + HMAC(32)
```

При стандартных email (20 символов) + пароль (12 символов):
```
EncCreds = Nonce(12) + Encrypted(EmailLen(2) + Email(20) + PassLen(2) + Pass(12)) + AuthTag(16)
         = 12 + 36 + 16 = 64 байта
```
**Итого: 22 + 32 + 8 + 2 + 64 + 32 = 160 байт** — характерный размер первого пакета.

**Оценка: 2/10** — набор фиксированных размеров создаёт надёжный fingerprint потока.

---

## 6. Паттерн установки соединения

```
Клиент → Сервер: ~160 байт (HandshakeInit)
Сервер → Клиент: ~200 байт (HandshakeResp, с PSK ~232)
Клиент → Сервер: 70 байт (HandshakeComplete)
           ... затем Data-пакеты ...
```

Это **уникальная 3-пакетная последовательность** с характерными размерами, не совпадающая ни с одним известным протоколом:

| Протокол | Паттерн соединения |
|----------|--------------------|
| TLS 1.2 | TCP SYN → SYN-ACK → ACK → ClientHello → ServerHello + Cert → ... (6+ пакетов) |
| QUIC | Initial(~1200B) → Initial+Handshake(~1200B) → Handshake → 1-RTT |
| DTLS 1.2 | ClientHello → HelloVerifyRequest → ClientHello → ServerHello → ... |
| **NovaVPN** | **~160B → ~200B → 70B** |

**Оценка: 2/10**

---

## 7. Статистический анализ Data-пакетов

### 7.1. Размеры IP-пакетов

Каждый VPN Data-пакет содержит ровно один IP-пакет. Overhead фиксирован: **+38 байт**. Это означает:

```
VPN-пакет = IP-пакет + 38
```

Распределение размеров VPN-пакетов **сдвигается на 38 байт** относительно типичного веб-трафика. Статистические методы (machine learning) могут обнаружить:
- Бимодальное распределение (ACK ~92B, данные ~1438B)
- Отсутствие пакетов менее 38B (кроме keepalive)
- Фиксированный сдвиг относительно MTU (1400 → 1438)

### 7.2. Отсутствие padding

В реализации `MaxPaddingSize = 32` объявлен, но **padding для Data-пакетов не используется**. Каждый пакет точно отражает размер инкапсулированного IP-пакета.

**Оценка: 5/10** — без ML тяжело, но при целенаправленном анализе — возможно.

---

## 8. Активное зондирование (Active Probing)

Если DPI-оператор подключается к серверу:

1. **Отправка случайных данных**: сервер молча отбрасывает (без TLS alert) — подозрительно
2. **Отправка TLS ClientHello**: сервер молча отбрасывает (настоящий TLS-сервер ответил бы ServerHello)
3. **Отправка QUIC Initial**: сервер молча отбрасывает
4. **Отправка HandshakeInit с нулевым PSK + фиктивные creds**: сервер ответит `Error: auth_failed`

Пункт 4 подтверждает наличие NovaVPN. При этом пакет Error:
```
Пакет Error: TLS_Header(5) + SessionID=0(4) + Type=0xF0(1) + Nonce=0(12) + "auth_failed"(11) = 33 байта
```
— незашифрованный, с фиксированным содержимым. **Одноразовый fingerprint.**

**Оценка: 1/10** — сервер не имеет fallback-поведения (например, проксирование на настоящий HTTPS).

---

## 9. Позитивные аспекты

| Аспект | Оценка | Комментарий |
|--------|--------|-------------|
| Рандомизация keepalive интервала | ✅ | 20-35s — хорошо для timing analysis |
| Порт 443 | ✅ | Стандартный, не вызывает подозрений |
| Шифрование payload | ✅ | ChaCha20-Poly1305 — содержимое нечитаемо |
| Ephemeral ключи | ✅ | Forward secrecy, нет постоянных маркеров |
| Отсутствие magic bytes | ✅ | Нет «NOVA» или подобных сигнатур |

---

## 10. Сводная оценка

| Критерий | Оценка | Вес | Взвешенная |
|----------|--------|-----|------------|
| Блокировка по порту | 8/10 | 5% | 0.40 |
| Простая DPI (первые байты) | 2/10 | 15% | 0.30 |
| Протокольная валидация (TLS over UDP?) | 1/10 | 25% | 0.25 |
| Stateful DPI (нет TLS handshake) | 1/10 | 20% | 0.20 |
| Fingerprint служебных пакетов | 2/10 | 15% | 0.30 |
| Статистический анализ | 5/10 | 10% | 0.50 |
| Active probing | 1/10 | 10% | 0.10 |

### Итоговая оценка стойкости к обнаружению: **3/10**

Протокол устойчив к самой базовой фильтрации (по порту, по первому байту `0x17`), но **обнаруживается любой системой DPI уровня ТСПУ, GFW или аналогичной** за счёт:
1. TLS 1.2 headers в UDP (не существует в природе)
2. Отсутствия TLS handshake перед Application Data
3. Фиксированных паттернов (нулевой SessionID + нулевой Nonce в первом пакете)

---

## 11. Предложения по улучшению

### 11.1. [Критично] Маскировка под реальный протокол

**Вариант A: Имитация DTLS 1.2** (используется в WebRTC, VoIP)

```
Заменить:     0x17 0x03 0x03  (TLS 1.2)
На:           0x17 0xFE 0xFD  (DTLS 1.2)
Добавить:     Epoch(2B) + SequenceNumber(6B) после ContentType+Version

Формат DTLS Record:
0x17  FE FD  [Epoch:2B]  [SeqNo:6B]  [Length:2B]  [Fragment...]
```

Преимущества:
- DTLS действительно работает поверх UDP
- Широко используется (WebRTC, VPN-клиенты Cisco AnyConnect)
- Провайдеру сложнее блокировать (сломает видеозвонки)

Сложность: средняя. Потребуется добавить 8 байт к заголовку.

**Вариант B: Маскировка под QUIC/HTTP3**

QUIC-пакеты имеют формат:
```
Short Header: [Header Form(1bit)=0] [Fixed Bit(1)=1] [Spin Bit] [Reserved(2)] [Key Phase] [Packet Number Length(2)]
              [Destination Connection ID(0-20B)]
              [Packet Number(1-4B)]
              [Payload...]
```

Преимущества:
- QUIC — доминирующий протокол на UDP/443 (Google, Cloudflare, Meta)
- Огромный объём легитимного трафика → сложно блокировать
- Short header минималистичный, легко имитировать

Сложность: высокая. Нужно имитировать Initial + Handshake фазы QUIC.

**Рекомендация: Вариант A (DTLS)** — значительное улучшение при минимальных изменениях.

### 11.2. [Критично] Убрать PacketType из открытой части

Сейчас: `SessionID(4) + Type(1) + Nonce(12) + Payload`

Предложение: перенести Type внутрь зашифрованной части.

```
БЫЛО:     SessionID(4) + Type(1) + Nonce(12) + Encrypt(Payload)
СТАЛО:    SessionID(4) + Nonce(12) + Encrypt(Type(1) + Payload)
```

Сервер определяет тип после расшифровки. Для HandshakeInit (SessionID=0) можно использовать отдельную логику — пробовать как handshake, если SessionID==0.

### 11.3. [Критично] Убрать нулевые паттерны

**SessionID=0 в первом пакете**: заменить на случайный «provisional SessionID»:
```go
provisionalID := rand.Uint32() | 0x80000000  // MSB=1 → provisional
```

Сервер по MSB=1 понимает, что это новый клиент.

**Нулевой Nonce в HandshakeInit**: заполнять случайными байтами:
```go
rand.Read(nonce[:])  // вместо [12]byte{}
```

Nonce в HandshakeInit не используется для шифрования, но его случайное значение устраняет fingerprint.

### 11.4. [Высокий приоритет] Padding для выравнивания размеров

Добавить случайный padding ко **всем** пакетам для маскировки размеров:

```go
// Выравнивание до ближайших 128 байт + случайный шум 0-64B
paddedSize := ((payloadLen / 128) + 1) * 128
randomExtra := rand.Intn(64)
totalPadding := paddedSize - payloadLen + randomExtra
```

Это устранит:
- Характерные фиксированные размеры Keepalive (22B), HandshakeComplete (70B)
- Прямую корреляцию VPN-пакет ↔ IP-пакет

### 11.5. [Высокий приоритет] Имитация TLS handshake (для варианта DTLS)

Перед передачей данных выполнить имитацию DTLS handshake:

```
Клиент → Сервер: [ContentType=0x16] Fake DTLS ClientHello (с SNI!)
Сервер → Клиент: [ContentType=0x16] Fake DTLS ServerHello + Certificate
Клиент → Сервер: [ContentType=0x14] Fake ChangeCipherSpec
                  [ContentType=0x16] Fake Finished
Сервер → Клиент: [ContentType=0x14] Fake ChangeCipherSpec
                  [ContentType=0x16] Fake Finished
            ... затем реальный NovaVPN handshake внутри 0x17 записей ...
```

Это скрывает протокол от stateful DPI, который ожидает TLS handshake перед Application Data.

### 11.6. [Средний приоритет] Fallback для active probing

При получении пакета, не соответствующего протоколу, **проксировать на настоящий веб-сервер**:

```
Клиент (DPI-зонд) → NovaVPN:443 (TLS ClientHello)
NovaVPN → proxy → Настоящий HTTPS:443 (например, nginx с обычным сайтом)
NovaVPN ← proxy ← Настоящий ответ (Server Hello + Certificate + ...)
Клиент ← NovaVPN: валидный HTTPS-ответ
```

Так при проверке порт 443 выглядит как обычный веб-сервер. Этот подход используется в Trojan, V2Ray (VLESS+XTLS), Shadowsocks-v2 plugin.

### 11.7. [Средний приоритет] Шифрование Error-пакетов

Сейчас Error отправляется open-text: `"auth_failed"`. Это fingerprint.

Варианты:
- Не отвечать на невалидные handshake вообще (молча отбрасывать)
- Или шифровать error тем же ephemeral session key

### 11.8. [Низкий приоритет] Randomized packet timing

Добавить случайную задержку к отправке Data-пакетов (0-5ms) для маскировки burst-паттернов. Снижает производительность, но затрудняет timing-анализ.

---

## 12. Приоритезированный план

| Приоритет | Улучшение | Эффект | Сложность |
|-----------|-----------|--------|-----------|
| 🔴 P0 | DTLS 1.2 заголовки вместо TLS 1.2 | +2 балла | Низкая |
| 🔴 P0 | Случайный Nonce и SessionID в Init | +1 балл | Низкая |
| 🔴 P0 | PacketType внутрь шифрованной части | +1 балл | Средняя |
| 🟠 P1 | Padding всех пакетов (кратно 128B) | +1 балл | Низкая |
| 🟠 P1 | Имитация DTLS handshake | +1.5 балла | Высокая |
| 🟡 P2 | Fallback-проксирование для probing | +1 балл | Высокая |
| 🟡 P2 | Шифрование/удаление Error-пакетов | +0.5 балла | Низкая |
| 🟢 P3 | Packet timing jitter | +0.3 балла | Низкая |

**При реализации P0 + P1: ожидаемая оценка 6-7/10**  
**При реализации всего: ожидаемая оценка 8-9/10**

---

## 13. Сравнение с существующими решениями

| Протокол | Маскировка | Стойкость к DPI |
|----------|-----------|----------------|
| OpenVPN (без обфускации) | Нет | 1/10 |
| WireGuard | Нет (свой UDP-формат) | 1/10 |
| **NovaVPN v2 (текущий)** | **TLS header over UDP** | **3/10** |
| Shadowsocks (AEAD) | Случайный поток | 5/10 |
| V2Ray (VMess) | Настраиваемое | 6/10 |
| Trojan | Настоящий TLS + HTTPS fallback | 8/10 |
| V2Ray (VLESS + XTLS-Vision) | Настоящий TLS + browser fingerprint | 9/10 |
| Tor (obfs4) | Полностью случайный вид | 7/10 |

---

## 14. Заключение

Текущая реализация маскировки **не выдержит проверку** современной DPI-системы (ТСПУ, GFW). Основная проблема — фундаментальное несоответствие: TLS 1.2 Record Headers на UDP-транспорте.

Тем не менее, протокол **уже лучше** чем OpenVPN/WireGuard без обфускации, и имеет хорошую архитектурную основу для улучшений. Реализация улучшений P0 (3 изменения, низкая/средняя сложность) даст значительный скачок стойкости при минимальных затратах.
