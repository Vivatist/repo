# Анализ идентифицируемости протокола NovaVPN на уровне провайдера

## Исполнительное резюме

**Вывод:** Протокол NovaVPN **крайне легко идентифицируется** на уровне провайдера с помощью техник Deep Packet Inspection (DPI).

**Уровень риска:** 🔴 **КРИТИЧЕСКИЙ** — блокировка может быть реализована за несколько часов.

---

## 1. Критические признаки идентификации

### 1.1. Магические байты в открытом виде

**Проблема №1 (критическая):**
```
Каждый пакет начинается с: 0x4E56 ("NV")
```

- **Расположение:** Первые 2 байта каждого UDP-пакета
- **Уникальность:** 100% — специфичны только для NovaVPN
- **Сложность детекции:** Тривиальная (одна BPF-фильтр)
- **Обход блокировки:** Невозможен без изменения протокола

**Пример BPF-фильтра для блокировки:**
```
udp and udp[8:2] = 0x4e56
```

### 1.2. Фиксированная структура заголовка

**Проблема №2 (критическая):**

Каждый пакет имеет строго определенную структуру:
```
Байты 0-1:   0x4E56 (магия)
Байт 2:      0x01 (версия)
Байт 3:      0x01-0x03, 0x10, 0x20, 0x30, 0xF0 (тип пакета)
Байты 4-13:  SessionID(4B) + Sequence(4B) + Length(2B)
Байты 14-25: Nonce (12 байт)
Байты 26+:   Зашифрованные данные + AuthTag
```

**Сигнатуры для детекции:**
- Первые 3 байта всегда `4E 56 01`
- 4-й байт из ограниченного набора значений
- Размер пакета = 14 (header) + 12 (nonce) + данные + 16 (auth tag)
- Минимальный размер keepalive/handshake пакетов: 26 байт + payload

### 1.3. Фиксированный порт по умолчанию

**Проблема №3 (высокая):**
- **Порт:** 51820/UDP
- **Проблема:** Это порт WireGuard, что привлекает внимание DPI-систем
- **Риск:** Дополнительный анализ трафика на этом порту

### 1.4. Предсказуемые паттерны трафика

**Проблема №4 (средняя):**

1. **Handshake-последовательность:**
   - Пакет типа 0x01 (HandshakeInit): ~200+ байт
   - Ответ типа 0x02 (HandshakeResp): ~160 байт
   - Пакет типа 0x03 (HandshakeComplete): ~60 байт
   - Строгая последовательность, всегда в начале соединения

2. **Keepalive-паттерн:**
   - Пакеты типа 0x20
   - Регулярные интервалы 25 секунд
   - Фиксированный размер: 26 байт (header+nonce) + минимальный payload

3. **Data-пакеты:**
   - Тип 0x10
   - Размеры типичны для IP-трафика: MTU ~1400 → пакеты ~1442 байта

---

## 2. Статистический анализ и fingerprinting

### 2.1. Энтропия данных

**Характеристика:** ChaCha20-Poly1305 дает высокую энтропию зашифрованного payload.

- ✅ **Плюс:** Данные выглядят случайными
- ❌ **Минус:** Это не помогает против сигнатур заголовков

### 2.2. Временные характеристики

**Уязвимые паттерны:**
1. Keepalive каждые 25 секунд — **очень характерно**
2. Handshake всегда предшествует data-трафику
3. Session timeout 120 секунд

### 2.3. Размерный анализ

**Детектируемые признаки:**
1. Handshake пакеты имеют характерные размеры (±10 байт):
   - Init: ~200 байт
   - Response: ~160 байт  
   - Complete: ~60 байт

2. Data пакеты: размер = IP_packet + 42 байта overhead

3. Keepalive: строго минимальный размер

---

## 3. Методы идентификации провайдером

### 3.1. Простейший метод (реализация за 1 час)

```python
# Псевдокод для DPI-системы
def is_novavpn_packet(udp_payload):
    if len(udp_payload) < 26:
        return False
    
    # Проверка магических байт
    if udp_payload[0:2] == b'\x4e\x56':
        # Проверка версии
        if udp_payload[2] == 0x01:
            # Проверка типа пакета
            if udp_payload[3] in [0x01, 0x02, 0x03, 0x10, 0x20, 0x30, 0xF0]:
                return True
    return False
```

**Точность:** >99.9% (false positive практически невозможен)

### 3.2. Продвинутый метод (статистический)

1. Сбор UDP-потоков по 5-tuple
2. Анализ первых 3-5 пакетов на наличие handshake-последовательности
3. Проверка регулярности keepalive (25 сек)
4. Анализ распределения размеров пакетов

**Точность:** >99.99%

### 3.3. ML-методы

Обучение классификатора на:
- Размеры пакетов
- Временные интервалы
- Энтропия
- Направленность трафика

**Точность:** ~99.9%, но излишни — простые сигнатуры работают отлично.

---

## 4. Сравнение с другими протоколами

| Протокол | Сложность детекции | Причина |
|----------|-------------------|---------|
| **NovaVPN** | 🔴 Тривиальная | Магические байты в открытом виде |
| OpenVPN | 🟡 Средняя | Сигнатура TLS handshake, сертификаты |
| WireGuard | 🟡 Средняя | Характерные паттерны handshake, фиксированные размеры |
| Shadowsocks | 🟢 Сложная | Выглядит как HTTPS (с obfs) |
| V2Ray + TLS | 🟢 Очень сложная | Неотличим от реального HTTPS |
| Tor (obfs4) | 🟢 Очень сложная | Обфускация специально разработана против DPI |

**Вывод:** NovaVPN в текущем виде **легче** детектировать, чем WireGuard.

---

## 5. Рекомендации по маскировке

### 5.1. Критические изменения (необходимый минимум)

#### Рекомендация 1: Удалить магические байты

**Текущее:**
```go
const ProtocolMagic uint16 = 0x4E56
```

**Предлагаемое решение:**
```go
// Убрать Magic из заголовка полностью
// Полагаться на AEAD для проверки подлинности
```

**Альтернатива (менее надёжная):**
```go
// Рандомизировать первые 2 байта, передавать как часть AAD
// Требует изменения парсинга
```

**Эффект:** Устраняет самый явный признак протокола.

#### Рекомендация 2: Рандомизировать структуру заголовка

**Текущее:** Фиксированная позиция полей

**Предлагаемое:**
- Добавить случайный padding (0-16 байт) между полями
- Использовать переменную позицию nonce
- Зашифровать весь заголовок (кроме минимума для роутинга)

**Пример:**
```
┌──────────────┬────────────────────────────────────────┐
│ SessionID 4B │  Encrypted(Header + Nonce + Payload)  │
└──────────────┴────────────────────────────────────────┘
```

#### Рекомендация 3: Изменить порт

**Действие:**
- НЕ использовать 51820 (порт WireGuard)
- Использовать порты популярных сервисов:
  - 443 (HTTPS) — лучший выбор
  - 853 (DNS-over-TLS)
  - 993 (IMAPS)
  - 8443 (альтернативный HTTPS)

**Эффект:** Снижает приоритет для DPI-анализа.

### 5.2. Дополнительные улучшения

#### Рекомендация 4: Имитация TLS

**Подход:** Обернуть протокол в TLS-подобный handshake

**Преимущества:**
- Выглядит как обычный HTTPS
- Проходит многие DPI-фильтры

**Реализация:**
1. Начать соединение с фейковым TLS ClientHello
2. Сервер отвечает фейковым ServerHello
3. Дальнейший трафик выглядит как Application Data

**Библиотеки:** 
- `utls` — имитация TLS клиентов браузеров
- Можно встроить реальный TLS и туннелировать внутри

#### Рекомендация 5: Рандомизация timing

**Текущее:** Keepalive строго каждые 25 секунд

**Предлагаемое:**
```go
// Случайный интервал 20-35 секунд
interval := 20 + rand.Intn(15)
```

**Эффект:** Усложняет статистический анализ.

#### Рекомендация 6: Padding для размеров пакетов

**Подход:** Добавлять случайный padding к пакетам

```go
// Добавить 0-32 байт случайного padding к каждому пакету
padding := make([]byte, rand.Intn(32))
rand.Read(padding)
payload = append(realPayload, padding...)
```

**Эффект:** Размывает сигнатуры размеров handshake-пакетов.

### 5.3. Радикальные решения

#### Рекомендация 7: Pluggable transports

**Подход:** Использовать проверенные обфускаторы

**Варианты:**
1. **obfs4** (из Tor):
   - Полиморфный протокол
   - Имитация случайного трафика
   - Защита от активного зондирования

2. **Cloak** (Shadowsocks-plugin):
   - Туннелирование через поддельный TLS
   - Имитация реального веб-сайта

3. **V2Ray транспорты:**
   - WebSocket + TLS + CDN
   - HTTP/2
   - QUIC

**Реализация:**
```
[NovaVPN] --> [obfs4] --> [Internet]
```

#### Рекомендация 8: Domain fronting / CDN tunneling

**Подход:** Туннелирование через CDN (Cloudflare, Azure, etc.)

**Преимущества:**
- Трафик неотличим от обычных HTTPS-запросов к CDN
- Блокировка потребует блокировки всего CDN

**Недостатки:**
- Более высокая латентность
- Зависимость от CDN-провайдера

---

## 6. Приоритетный план внедрения

### Фаза 1: Критические исправления (1-2 дня)

1. ✅ **Удалить ProtocolMagic** из заголовка
2. ✅ **Изменить порт** на 443
3. ✅ **Рандомизировать keepalive** интервалы

**Эффект:** Блокировка потребует более сложного DPI.

### Фаза 2: Структурные изменения (1 неделя)

4. ✅ Зашифровать заголовок (оставить только SessionID открытым)
5. ✅ Добавить случайный padding к пакетам
6. ✅ Переменная позиция nonce

**Эффект:** Протокол становится похож на зашифрованный трафик без явных сигнатур.

### Фаза 3: Продвинутая маскировка (2-3 недели)

7. ✅ Реализовать TLS-обёртку (опциональная)
8. ✅ Интеграция с obfs4 или аналогом
9. ✅ Режим туннелирования через WebSocket

**Эффект:** Протокол неотличим от легитимного HTTPS/WSS трафика.

---

## 7. Дополнительные меры безопасности

### 7.1. Защита от активного зондирования

**Проблема:** Провайдер может отправлять пробные пакеты на сервер.

**Решение:**
- Не отвечать на некорректные handshake
- Rate limiting по IP
- Требовать валидный PSK для любого ответа
- Timeout перед ответом (anti-fingerprint)

### 7.2. Защита от replay-атак

**Текущее:** Timestamp ±30 сек (HandshakeInit)

**Усиление:**
- Сохранять nonce/timestamp использованных пакетов
- Sliding window для SequenceNo

### 7.3. Распределённая инфраструктура

**Рекомендации:**
- Несколько серверов на разных IP/портах
- Ротация адресов
- Использование bridge-серверов (как в Tor)

---

## 8. Тестирование маскировки

### 8.1. Инструменты для проверки

```bash
# 1. Проверка магических байт
tcpdump -i any -X 'udp and dst port 51820' | grep '4e56'

# 2. Сбор статистики размеров пакетов
tshark -r capture.pcap -T fields -e frame.len \
    -Y 'udp.port == 51820' | sort -n | uniq -c

# 3. Анализ временных интервалов
tshark -r capture.pcap -T fields -e frame.time_relative \
    -Y 'udp.port == 51820'

# 4. Проверка энтропии
tcpdump -w - 'udp and port 51820' | \
    ent -c  # Должно быть ~7.99 bits/byte для хорошего шифрования
```

### 8.2. Публичные DPI-сервисы

Тестирование против:
- **GFW (Great Firewall of China)** — симулятор
- **DPI-тестеры:** Wireshark + плагины для детекции VPN
- **nDPI** — open-source DPI engine

---

## 9. Выводы

### Текущее состояние

| Критерий | Оценка | Комментарий |
|----------|--------|-------------|
| Скрытность | 🔴 1/10 | Тривиально детектируется |
| Безопасность крипто | 🟢 9/10 | Современная криптография |
| Производительность | 🟢 9/10 | UDP + ChaCha20 = быстро |
| Надёжность | 🟢 8/10 | Handshake + keepalive работают хорошо |

### После внедрения рекомендаций

**Фаза 1 (критические исправления):**
- Скрытность: 🟡 4/10
- Требуется специализированная сигнатура

**Фаза 2 (структурные изменения):**
- Скрытность: 🟢 7/10
- Детекция требует глубокого статистического анализа

**Фаза 3 (продвинутая маскировка):**
- Скрытность: 🟢 9/10
- Неотличим от HTTPS, блокировка экономически нецелесообразна

---

## 10. Заключение

**Основная проблема:** NovaVPN в текущем виде имеет **уникальную неизменную сигнатуру** (магические байты 0x4E56), что делает его **тривиально детектируемым** любой DPI-системой.

**Уровень угрозы:** В странах с активной интернет-цензурой (Китай, Иран, Россия, Туркменистан) протокол может быть заблокирован **в течение нескольких часов** после обнаружения.

**Необходимые действия:** Внедрение как минимум **Фазы 1 рекомендаций** критически важно для использования в условиях DPI-мониторинга.

**Долгосрочная стратегия:** Для максимальной устойчивости к блокировкам необходимо реализовать **Фазу 3** (TLS-обёртка или pluggable transports).

---

**Дата анализа:** 2026-02-11  
**Версия протокола:** 0x01  
**Статус:** 🔴 Требуются критические изменения
